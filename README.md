# 🤖 Telegram Admin Bot

یک ربات تلگرام قدرتمند برای مدیریت چندین اکانت تلگرام و ایجاد خودکار گروه‌ها.

## 📋 معرفی پروژه

این ربات امکان مدیریت چندین اکانت تلگرام را از طریق یک پنل ادمین فراهم می‌کند. ربات به صورت خودکار با استفاده از اکانت‌ها گروه ایجاد کرده و اطلاعات و آمار را مدیریت می‌کند.

## ✨ ویژگی‌ها

- 📂 **مدیریت اکانت‌ها**: افزودن، حذف، فعال/غیرفعال کردن اکانت‌ها
- 🔄 **زمان‌بندی خودکار**: ایجاد خودکار گروه‌ها در بازه‌های زمانی مشخص
- 📊 **آمار و گزارش**: مشاهده آمار کلی اکانت‌ها و گروه‌های ایجاد شده
- ⚠️ **لاگ خطاها**: ثبت و نمایش خطاهای رخ داده
- 🌐 **پشتیبانی از پروکسی**: تنظیم پروکسی اختصاصی برای هر اکانت
- 📁 **مدیریت سشن‌ها**: دریافت فایل سشن هر اکانت
- 💾 **پایگاه داده**: ذخیره‌سازی اطلاعات در SQLite

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8+
- Telegram API ID و API Hash (از my.telegram.org)

### مراحل نصب

1. کلون کردن پروژه:
```bash
git clone <repository-url>
cd <project-directory>
```

2. نصب وابستگی‌ها:
```bash
pip install -r requirements.txt
```

3. تنظیمات فایل `config.py`:
```python
API_ID = 16623           # API ID شما از my.telegram.org
API_HASH = "your_hash"   # API Hash شما
BOT_TOKEN = "your_bot_token"  # توکن ربات ادمین

# لیست ID کاربرانی که اجازه استفاده دارند
ADMIN_IDS = [123456789, 987654321]
```

4. اجرای ربات:
```bash
python main.py
```

## 📱 منوهای ربات

### 🏠 منوی اصلی

دستور `/start` یا ارسال هر پیام به ربات منوی اصلی را نمایش می‌دهد:

```
🛠 Admin Panel
Choose an action:

┌─────────────────────┐
│  📂 Accounts        │  📊 Stats       │
│  ⚠️ Errors          │  ⏱ Scheduler   │
│  ➕ Add Account     │                │
└─────────────────────┘
```

### 📂 مدیریت اکانت‌ها

#### لیست اکانت‌ها
با کلیک روی `📂 Accounts` لیست تمام اکانت‌ها نمایش داده می‌شود:
- نمایش در قالب جدول 5x2 (10 اکانت در هر صفحه)
- پشتیبانی از صفحات‌بندی (Pagination)
- نمایش وضعیت با رنگ: 🟢 فعال / 🔴 غیرفعال
- شماره اکانت و نام نمایشی

```
📂 Accounts (page 1/2):

┌──────────────────────────────────────┐
│  🟢 Acc 1    │  🟢 Acc 2           │
│  🟢 Acc 3    │  🔴 Acc 4           │
│  ...          │  ...                │
│  ◀️ Prev      │  Next ▶️           │
└──────────────────────────────────────┘
```

#### جزئیات اکانت
با کلیک روی هر اکانت، جزئیات آن نمایش داده می‌شود:

```
👤 Account: +989123456789
ID: 1
Status: Active 🟢
Groups Created: 45
Proxy: 192.168.1.1:8080

┌──────────────────────────────────────┐
│  Turn Off     │  Set Proxy          │
│  Delete Account│  Send Session File  │
│  ⬅️ Back to list                    │
└──────────────────────────────────────┘
```

**عملیات موجود:**

1. **Turn On/Off**: فعال یا غیرفعال کردن اکانت
2. **Set Proxy**: تنظیم پروکسی برای اکانت
   - فرمت: `host:port` یا `host:port:user:pass`
   - برای حذف پروکسی: ارسال `none`

3. **Delete Account**: حذف اکانت (نیاز به تایید دارد)
4. **Send Session File**: دریافت فایل سشن اکانت

### ➕ افزودن اکانت جدید

با کلیک روی `➕ Add Account` فرآیند افزودن شروع می‌شود:

**مرحله 1**: وارد کردن شماره تلفن
```
Please enter the phone number (including country code, e.g., +989123456789):
```

**مرحله 2**: وارد کردن کد تایید
```
Code sent to +989123456789. Please enter the code:
```

**مرحله 3**: اگر احراز هویت دو مرحله‌ای فعال باشد:
```
Two-step verification enabled. Please enter your password:
```

در صورت موفقیت:
```
✅ Account +989123456789 added successfully!
```

### 📊 آمار کلی

نمایش آمار کلی سیستم:

```
📊 Global Stats
Total accounts: 10
Active accounts: 8
Total groups created (all accounts): 1250
```

### ⚠️ مشاهده خطاها

نمایش 10 خطای اخیر:

```
⚠️ Latest Errors:

[2024-01-15 10:30:45] Account=1 | scheduler_create_group
FloodWaitError: A wait of 30 seconds is required

[2024-01-15 10:25:12] Account=2 | channels_create
ChannelsTooMuchError: Too many channels
```

### ⏱ مدیریت زمان‌بندی (Scheduler)

کنترل زمان‌بند خودکار:

```
⏱ Scheduler Status: 🟢 Running

┌─────────────────────┐
│  ▶️ Start    │  ⏹ Stop   │
└─────────────────────┘
```

- **▶️ Start**: شروع زمان‌بند
- **⏹ Stop**: توقف زمان‌بند

## 🗄️ ساختار پایگاه داده

### جدول accounts
| فیلد | نوع | توضیح |
|------|------|-------|
| id | INTEGER | شناسه یکتا |
| label | TEXT | نام نمایشی اکانت |
| session_path | TEXT | مسیر فایل سشن |
| is_active | INTEGER | وضعیت فعال (1/0) |
| created_groups_count | INTEGER | تعداد گروه‌های ایجاد شده |
| first_activity_at | TEXT | تاریخ اولین فعالیت |
| last_group_created_at | TEXT | تاریخ آخرین گروه |
| proxy_host | TEXT | هاست پروکسی |
| proxy_port | INTEGER | پورت پروکسی |
| proxy_username | TEXT | نام کاربری پروکسی |
| proxy_password | TEXT | رمز عبور پروکسی |

### جدول groups
| فیلد | نوع | توضیح |
|------|------|-------|
| id | INTEGER | شناسه یکتا |
| account_id | INTEGER | شناسه اکانت |
| chat_id | TEXT | شناسه چت تلگرام |
| title | TEXT | عنوان گروه |
| created_at | TEXT | تاریخ ایجاد |
| messages_sent | INTEGER | تعداد پیام‌ها |

### جدول errors
| فیلد | نوع | توضیح |
|------|------|-------|
| id | INTEGER | شناسه یکتا |
| account_id | INTEGER | شناسه اکانت |
| context | TEXT | زمینه خطا |
| error_text | TEXT | متن خطا |
| created_at | TEXT | تاریخ ثبت |

## ⏰ رفتار زمان‌بند (Scheduler)

زمان‌بند به صورت خودکار در پس‌زمینه اجرا می‌شود و عملیات زیر را انجام می‌دهد:

### پارامترهای تنظیم (در config.py)
```python
GROUP_INTERVAL_MINUTES = 1    # فاصله زمانی بین ایجاد گروه‌ها
MAX_GROUPS_PER_ACCOUNT = 450  # حداکثر گروه برای هر اکانت
MAX_ACCOUNT_DAYS = 10         # حداکثر عمر اکانت (روز)
```

### عملکرد
1. بررسی اکانت‌های فعال
2. ایجاد گروه برای اکانت‌های واجد شرایط
3. ارسال 10 پیام حاوی اطلاعات زمان به هر گروه
4. بروزرسانی آمار و پایگاه داده
5. مدیریت خطاها و Flood Wait

### نام‌گذاری گروه‌ها
فرمت نام گروه‌ها:
```
ACC01 • G001 • 2024-01-15
```

### پیام‌های ارسال شده به گروه
هر گروه 10 پیام با اطلاعات زمان دریافت می‌کند:
- Year: سال
- Month: ماه
- Day: روز
- Weekday: روز هفته
- Hour: ساعت
- Minute: دقیقه
- Second: ثانیه
- ISO: فرمت ISO
- Unix: تایم‌استمپ یونیکس
- Summary: خلاصه تاریخ و زمان

## 🔧 تنظیمات پیشرفته

### پروکسی

برای هر اکانت می‌توان پروکسی اختصاصی تنظیم کرد:

**پروکسی SOCKS5 بدون احراز هویت:**
```
192.168.1.1:1080
```

**پروکسی SOCKS5 با احراز هویت:**
```
192.168.1.1:1080:username:password
```

حذف پروکسی:
```
none
```

### Device Model (در accounts.py)

برای جلوگیری از شناسایی اکانت‌ها، از Device Model یکنواخت استفاده می‌شود:
```python
device_model="POCO POCO X6 Pro 5G"
system_version="Android 15"
app_version="11.13.0.1"
lang_code="en"
system_lang_code="en"
```

## 📂 ساختار فایل‌ها

```
project/
├── main.py           # نقطه شروع برنامه
├── admin_bot.py      # هندلرهای ربات ادمین
├── scheduler.py      # زمان‌بند خودکار
├── accounts.py       # مدیریت اکانت‌ها و کلاینت‌ها
├── db.py            # عملیات پایگاه داده
├── config.py        # تنظیمات
├── utils.py         # توابع کمکی
├── requirements.txt # وابستگی‌ها
├── data.db          # پایگاه داده (ایجاد می‌شود)
└── sessions/        # فایل‌های سشن (ایجاد می‌شود)
```

## 🐛 مدیریت خطاها

### انواع خطاهای رایج

1. **FloodWaitError**: زمان انتظار مجبور - ربات به صورت خودکار منتظر می‌ماند
2. **ChannelsTooMuchError**: اکانت به محدودیت گروه رسیده - اکانت غیرفعال می‌شود
3. **PhoneCodeInvalidError**: کد تایید نامعتبر
4. **SessionPasswordNeededError**: نیاز به رمز عبور دو مرحله‌ای

### اطلاع‌رسانی خطاها
- خطاهای حیاتی به ادمین‌ها از طریق ربات ارسال می‌شوند
- همه خطاها در پایگاه داده ذخیره می‌شوند
- می‌توان از بخش ⚠️ Errors لیست خطاها را مشاهده کرد

## 🔐 امنیت

- فقط کاربران با ID مشخص در `ADMIN_IDS` می‌توانند از ربات استفاده کنند
- فایل سشن‌ها به صورت امن ذخیره می‌شوند
- توکن API و Bot در فایل config.py باید محافظت شوند

## 📝 مثال استفاده

1. **شروع ربات**:
```bash
python main.py
```

2. **افزودن اکانت جدید از طریق تلگرام**:
- ارسال `/start` به ربات
- کلیک روی `➕ Add Account`
- وارد کردن شماره تلفن: `+989123456789`
- وارد کردن کد تایید
- وارد کردن رمز عبور (در صورت نیاز)

3. **تنظیم پروکسی**:
- باز کردن جزئیات اکانت
- کلیک روی `Set Proxy`
- ارسال: `192.168.1.1:1080:user:pass`

4. **شروع زمان‌بند**:
- کلیک روی `⏱ Scheduler`
- کلیک روی `▶️ Start`

## 🤝 پشتیبانی

برای گزارش مشکلات و پیشنهادات، لطفاً Issue ایجاد کنید.

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 🔗 منابع مفید

- [Telethon Documentation](https://docs.telethon.dev/)
- [Telegram API](https://core.telegram.org/api)
- [my.telegram.org](https://my.telegram.org)
